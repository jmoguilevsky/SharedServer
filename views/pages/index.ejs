<!DOCTYPE html>
<html ng-app="userApp">

<header>

  <% include ../partials/header.ejs %>
  <link rel="stylesheet" type="text/css" href="/public/stylesheets/main.css" />
  <script src="controllers/usercontroller.js"></script>

</header>
<body ng-controller="UserListController">
  <% include ../partials/nav.ejs %>

<!--
  <ul>
    <li ng-repeat="item in users">
      <span> {{item.user.name}} </span>
      <p> {{item.user.alias}} </p>
    </li>
  </ul>
-->
<div class="list-group">
  <a ng-repeat="item in users" href="#" class="list-group-item"> 
    <h4 class="list-group-item-heading">{{item.user.name}} </h4>
    <p class="list-group-item-text">{{item.user.alias}}</p>
  </a>
</div>


</body>
</html>
